specVersion: "v1beta"
serviceId: "contenthub"
location: "asia-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "contenthub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
